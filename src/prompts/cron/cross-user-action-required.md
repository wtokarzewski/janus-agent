ACTION REQUIRED — you MUST send TWO messages now:
1. Notify TARGET user "{{targetUserId}}" that a reminder "{{name}}" was set for them (include what and when).
2. Confirm to REQUESTER "{{requesterId}}" that the reminder is active.
When the reminder fires and the target user completes or dismisses it, notify the requester about the outcome.